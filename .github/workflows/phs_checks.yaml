on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

name: phs_checks.yaml

permissions: 
  contents: read
  pull-requests: read

jobs:
  PHS-checks:
    uses: Public-Health-Scotland/actions/.github/workflows/phs_package_checks.yaml@add-pkgdown
    with:
      limit_parallel: true
    permissions: 
      contents: write
      pull-requests: write
    secrets: inherit

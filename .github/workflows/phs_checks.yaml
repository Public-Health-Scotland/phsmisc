on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

name: phs_checks.yaml

permissions: 
  contents: read
  pull-requests: read
  statuses: read

jobs:
  PHS-checks:
    uses: Public-Health-Scotland/actions/.github/workflows/phs_package_checks.yaml@fix_commit_permission_issues
    permissions: 
      contents: write
      pull-requests: write
      statuses: write
    secrets: inherit
